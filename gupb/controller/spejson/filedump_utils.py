# Well, this file exists in two versions. One that is doing nothing; and the other that saves the feature maps.
# The latter are meant for running in colab, where you cannot destroy anything and is dynamically inserted here.
# You don't see this? Good! It was supposed to be done this way.

def create_dump_folder(character_name):
    pass


def dump_numpy_array(dest_path, state_tensor):
    pass
